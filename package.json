{
  "name": "onyx-biopartners-site",
  "version": "1.0.0",
  "private": true,
  "description": "Onyx BioPartners marketing site",
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve",
    "build:vite": "vite build",
    "build:eleventy": "eleventy",
    "build": "npm run build:vite && npm run build:eleventy && npm run validate:ld",
    "validate:ld": "node scripts/validate-ld.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sharp": "^0.34.5",
    "vite": "^5.4.10"
  }
}
